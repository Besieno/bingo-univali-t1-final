# Documentos da versão ANTERIOR (09/09/2026)

⚠️ **Estes arquivos descrevem um código que não está mais em `src/`.**

Em 10/09/2026 o repositório foi atualizado para a **versão do grupo** (`main (6).cpp`),
que partiu da base do João e seguiu num caminho diferente. Os documentos aqui descrevem
a versão de 09/09 — aquela que tinha `inserirEmOrdem`, `getline` nos nomes e
`lerOpcao` com `cin.clear()`.

**Para que servem ainda:**

1. **Defesa.** Várias explicações continuam valendo palavra por palavra, porque as funções
   são as mesmas em espírito: `gerarCartela`, `bubblesort`, `cartelaCompleta`, `pintarNumero`,
   `gotoxy`/`ansiBG`. Se o professor perguntar sobre uma dessas, a resposta está aqui.
2. **Comparação.** Se alguém perguntar "por que vocês não fizeram assim?", aqui está a versão
   que fazia — e o porquê de cada decisão dela.

O código dessa versão está preservado em `historico/`:
- `main-09-09-2026-arquivo-unico-original.cpp` — o arquivo único de 478 linhas
- `bingo-entrega-anterior-09-09.cpp` — o que tinha sido gerado para entrega
- `MUDANCAS-da-versao-anterior.md` — o que mudou da versão do João (09/09) para ela

**Para a versão que está no ar agora**, use `funcoes/` (o que cada função faz) e
`docs/91-o-que-pode-ser-contestado.md` (o que ainda falta).
