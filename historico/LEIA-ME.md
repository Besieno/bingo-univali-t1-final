# Histórico

## `main-09-09-2026-arquivo-unico-original.cpp`

É **o arquivo como ele existia em 09/09/2026**, antes de ser separado em partes:
478 linhas, tudo num arquivo, já com o jogo completo e testado.

Está aqui por dois motivos:

1. **Rastreabilidade.** É o artefato que foi efetivamente rodado e validado naquele dia.
   Se algum dia houver dúvida sobre "o que exatamente estava funcionando", é este arquivo.
2. **Prova.** As 21 funções dele foram comparadas, ignorando espaço e comentário, com os
   arquivos de `src/` e com o `entrega/bingo.cpp` gerado: **todas batem, e nenhuma linha
   de código ficou de fora**. Ou seja, separar em partes não mudou o programa.

> **Não edite este arquivo.** Ele é um registro, não código vivo. O código vivo é `src/`;
> o que vai ser entregue é `entrega/bingo.cpp`, gerado a partir de `src/`.

O `MUDANCAS.md` na raiz conta o que mudou **antes** disso: da versão do João (09/09,
`main (5).cpp`) para este arquivo, item por item, com o porquê.
